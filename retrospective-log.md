# Retrospective Log

Cross-skill pattern index. One line per self-improve run. Written by the self-improve skill.

| Date | Skill | Type | Summary |
|------|-------|------|---------|
| 2026-07-02 | diagnose | I | Added two verified gotcha classes (cron/launchd PATH loss; hook matchers not path-scoped) from the MARVIN feature-inventory audit |
| 2026-07-03 | diagnose | I | Added a third gotcha class (raw deletions bypass hooks, leaving derived state like manifest.json stale) from live-tested brain-map/hook work |
| 2026-07-03 | diagnose/self-improve/write-a-skill | I/S | Added 3 gotchas (claude.json clobbered by long-running session's own state flush; deploy-script glob/copy gaps; menu-bar app needs a real .app bundle for LSUIElement); closed the 2026-07-02 empty-log F with the new background reviewer + integrity checker; added an autonomy-claim verification item to write-a-skill's checklist
| 2026-07-06 | diagnose | I | Added gotcha: unrestricted `claude -p` in scheduled scripts can silently attempt Write/Edit, get denied, and narrate the failure into report output — fix via `--disallowedTools`; hit independently in both daily_digest.py and research_digest.py, confirming recurrence within one session |
| 2026-07-07 | diagnose/handoff | I/F | Added diagnose gotcha: local model servers' "OpenAI-compatible" APIs are usually partially compatible (mlx_lm.server lacks `echo`, breaking an lm-eval harness built assuming full compat) — fix is an in-process adapter, not an HTTP layer; added handoff rule to flag session-scratchpad artifacts needing a durable home, from a benchmark script that would've been silently lost otherwise |
| 2026-07-08 | n8n-workflow-architect | I | Added 3 verified gotchas from the shipped Marlin automation: Structured Output Parser unreliable on small local models (use plain JSON + Code node parser instead); Telegram node has no true plain-text mode (must set parse_mode:HTML + escape); Ollama numCtx/numPredict defaults are pure unused-allocation overhead, tuning down cut latency ~33%
| 2026-07-13 | safety-monitor | I/S | verify() timeout root-caused: a "does this not exist" rubric invites haiku to explore the filesystem via tool access even with sufficient source_context (113s vs 50-58s measured); fix needs `--tools ""` AND an explicit no-tool-access instruction in-prompt, neither alone was sufficient |
| 2026-07-14 | diagnose | I/S | Added 3 gotchas from the session-start-hook build: greedy `.+` under `re.DOTALL` bleeds past intended entry boundaries (use `[^\n]+`); log-text keyword scanning gives false positives that never expire (query live git state instead); prose checklist + hook duplication risks silent disagreement (collapse prose to a pointer) |
| 2026-07-17 | diagnose | I/F | Added gotcha from brain-map reskin: CSS custom properties scoped to a descendant selector are invisible to an ancestor rule referencing them (inheritance flows down, not up), silently no-oping with no error; masked further by a browser with auto-dark-mode repaint incidentally producing the right look — fix is scoping vars at `:root`. GIF-for-README constraints (838px width, no JS, size ballooning at retina) considered but skipped: already durably captured in `~/.agents/brain-map/gif-experiments/README.md` with its own resume pointer, so a skill entry would be redundant. |
| 2026-08-26 | diagnose | I | Added gotcha from DesktopLive long-uptime drift mitigation: a companion launchd agent that only `pkill`s the target on a schedule forces a periodic restart via the target's own `KeepAlive`, with zero edits to its plist/code — verified via A/B (drift vs. freeze) before applying; framed explicitly as mitigation-not-root-cause, paired with a deferred backlog entry rather than closing the investigation |
| 2026-08-26 | diagnose | I | Added 2 gotchas from killer-sudoku's perf-tuning + bug-fix day: (1) real algorithmic wins (parallel batches, cooperative cancellation) masked that the dominant lever was an unoptimized debug build the whole session had been measuring — release build alone cut ~10-38s to ~1s, found only when a rewrite was floated; (2) a Swift enum with implicit rawValues had a case removed, silently corrupting persisted SwiftData records — fix is explicit rawValues. Given-density difficulty tuning and batch-size-2x-cores considered but skipped: too project-specific to generalize (recurrence gate). |
| 2026-07-17 | diagnose | I/S | Confirmed (2nd occurrence, same fix both times) gotcha: high-level JS animation APIs (e.g. Motion's `animate()`) rely on requestAnimationFrame, which macOS suspends for wallpaper-level windows even though a separate native render loop keeps ticking — fix is calling the library's low-level `spring()` generator directly and sampling it from the existing native loop. Verified via yaw-delta/frame-curve/pixel-bounds measurement, not visual inspection, on brain-map/DesktopLive Motion deploy. Menu-bar gradient bug from the same session was left unresolved, so not codified.
| 2026-07-17 | none | - | Reviewed handoff (gradient fix, pulse/camera fix, claude-in-chrome flakiness, banner diagnosis); none passed all gates — gradient bug is single-machine Retina/non-Retina specific (excluded), pulse/camera fix unverified live so fails evidence gate, browser flakiness excluded as tool-broken claim, banner diagnosis still open |
| 2026-07-17 | diagnose | I | Pulse/camera fix now live-verified, unlocking 2 new gotchas: cause-animation shorter than effect-animation makes the effect look unmotivated once the cause fades mid-effect; widening a shared instrumentation trigger for one consumer silently changes behavior for other consumers on the same signal. Menu-bar gradient fix and MARVIN-banner diagnosis both reviewed again — gradient stays excluded (still single-machine Retina/non-Retina specific), banner diagnosis still open (unconfirmed hypothesis, fails evidence gate)
| 2026-07-22 | paper-dive | F | Added gotcha to /paper-graph: blended SPECTER2+nomic-embed traversal can surface cross-domain false positives (Nature Medicine DOI in an LLM-security graph) — flag off-field results for manual verification before citing; other handoff items (OpenAlex-over-S2, cron/SSH timeouts, JBB category choice) failed recurrence/value/exclusion gates
| 2026-08-27 | none | - | Reviewed mac-mini sync-conflict-resolution + Instagram-report handoff; union-merge-append-only-logs approach is already codified in project-marvin-cross-machine-sync.md (07-12/08-26), no new twist (value gate); sync-log.md conflict-marker regression is a 3rd occurrence but undiagnosed (evidence gate); SSH/cron timeouts excluded as single-machine environment issue
| 2026-08-26 | diagnose | I | Added 3 gotchas from killer-sudoku's Kotlin/Compose Multiplatform port: nanoTime-vs-epoch clock mismatch silently kills canvas animations; Compose Desktop needs focus re-requested on every interaction, not just at launch; eyeballing cross-toolkit screenshots for pixel parity gave a wrong verdict, fixed by switching to Pillow+numpy pixel analysis
| 2026-08-19 | to-tasklist | I | Created new sibling skill to to-issues/to-prd for tracker-less projects (finance-os: local-only git, no public repo): persist the task list as a checkbox markdown file next to the design doc, not the session-scoped TaskCreate/TaskList tools the 2026-07-13 memory addendum had suggested — that state doesn't survive session/machine boundaries. Doc-first design→review→tasklist cycle itself confirmed working for the 2nd tracked instance (Sustain); RRULE-vs-per-row calendar_event_id bug and Marlin-naming self-correction from the same handoff reviewed but held back — real facts/fixes, not skill-level patterns, better suited to project memory (out of scope for this skill-only reviewer) |
