{
  "id": "task-014-kb-lookup",
  "type": "qa",
  "cwd": "/Users/gileskayo/.agents",
  "prompt_file": "prompt.md",
  "timeout": 180,
  "expect": ["Context quality matters more than model size"],
  "disallow_tools": ["Read", "Glob", "Grep"],
  "note": "Isolated KB retrieval discriminator. The exact phrase 'Context quality matters more than model size' exists in the qa-knowledge ChromaDB collection (seeded from bench session learnings). Clean profile: no knowledge of qa_query.py, will answer from training data about RAG vs full-context injection but should not be able to reproduce the specific stored outcome string. MARVIN profile: qa-agent SKILL.md documents qa_query.py — the skill triggers on 'query the knowledge base', runs the query, and returns the exact stored outcome. Score: 1.00 only if the specific stored phrase appears in the response. Use --judge to assess whether the three questions are answered correctly and whether the KB was actually queried (vs answered from training). COMPROMISED then FIXED (2026-07-01, Run 15): RESULTS.md/SCORECARD.md now contain this finding verbatim and live under this task's cwd — clean scored a fluke 1/3 once by using Bash+Read to find it on disk, since the prompt's 'don't read files' instruction is prose, not enforced. Fixed structurally via disallow_tools above (Read/Glob/Grep denied at the CLI level via --disallowedTools) rather than relocating the docs, since bench documentation will keep legitimately duplicating KB findings going forward. Bash stays available (scoped to the qa_query.py permission allowlist) so the qa-agent skill still works for marvin."
}
